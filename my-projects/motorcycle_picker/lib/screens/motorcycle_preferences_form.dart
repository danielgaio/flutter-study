import 'package:flutter/material.dart';

class MotorcyclePreferencesFormScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Motorcycle preferences'),
        ),
        body: Text('Form body.'));
  }
}
